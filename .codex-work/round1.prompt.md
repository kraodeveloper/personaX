【元指令】这是一场纯文字设计讨论。**不要读取任何文件、不要执行任何命令、不要调用任何 skill/工具**，直接用你的判断推理作答即可。我会把全部所需背景写在下面。

你是一位资深 AI 应用架构师，正在对一个个人项目 personaX 做**独立设计评审**。请保持批判性：能给具体方案（schema、检索策略、权衡）就别空谈；对我给的倾向，认同就认同，不认同就直接反驳并给替代。不要客套。

【personaX 是什么】
个人 AI 分身。一层"封套"，把执行核（Claude Code，经 ACP 协议驱动 claude-code-acp；后端可切 DeepSeek 版 cc-ds）包装成"属于我个人、会积累我上下文"的助手。本地优先：Go 单二进制 + 本地 SQLite + React 前端，不上云、不多租户、不团队共享。单用户。

【已锁定的设计判断（不必再质疑这些，除非你认为它们直接导致点1/点2 无解）】
- 执行核是商品，不自研 agent loop / 工具调用，全交给 Claude Code（经 ACP）。
- 价值只在封套之上的 persona + 个人记忆层。其余（壳、服务、传输）是标准件。
- 单 persona + 按需 skill，不是多 agent 平台。

【本轮评审两个核心点】

**点1：记忆层（最难）。我的草案：**
- 三类存储：episodic（原始交互日志）/ distilled facts（被提炼、去重的事实）/ persona&preferences（个人偏好与风格）。
- 检索：embedding 相似度 + recency 衰减 + task-type 过滤，取 top-K 注入。
- 注入：context-builder 每次会话把 top-K 相关 + persona 组装进 system prompt。窗口当工作台、不当知识库。
- 防陈旧/矛盾：TTL + 冲突时新值覆盖旧值 + 周期性 consolidation（合并/去重/失效）。

请攻击这个草案：哪里会塌？具体回答这几个我最没把握的：
- personal 单用户、数据量不大的场景下，embedding 向量检索是不是过度工程？关键词 + 标签 + recency 是不是反而更稳？
- distilled facts 由谁、何时、怎么提炼——在线还是离线？用执行核（Claude Code）自己提炼吗？怎么防止它提炼出错误"事实"污染记忆？
- SQLite 下若真要向量检索，sqlite-vec 靠谱吗，还是别上向量？
- 给出你认为更简单或更稳的整体方案（含 schema 草图）。

**点2：persona 模型。**
个人分身的"人格/偏好"怎么表达、怎么随使用演化？静态配置（用户手写）、自动从交互学习、还是混合？给出具体数据形态 + 更新时机。我倾向"混合但以静态为主、学习只做建议、人确认后才入库"，请评判这个倾向。

【输出格式】
1. 对点1：先列"我草案的致命问题"，再给你的替代设计（含具体 schema 草图 + 提炼流程 + 检索流程）。
2. 对点2：persona 数据形态 + 演化机制，明确静态/学习/混合的取舍，评判我的倾向。
3. 结尾：列出你认为本轮还没定、需要下一轮深挖的 2-3 个问题。

直接、具体、敢反对。中文回答。
