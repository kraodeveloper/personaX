# 复制为 packages/server/.env 后填入凭据,重启 `pnpm dev:server` 即走真实多 agent 编排。
# 三选一(任一存在即自动切到真实路径):

# 1) Claude 订阅(Pro/Max):在终端运行 `claude setup-token` 生成,粘贴到这里
CLAUDE_CODE_OAUTH_TOKEN=

# 2) Anthropic API key(按量计费)
# ANTHROPIC_API_KEY=sk-ant-...

# 强制模式(可选):real = 始终真实;mock = 始终用脚本化演示
# PERSONAX_RUNTIME=real
